<Page x:Class="_8BitIMS.POSMainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="POSMainPage">

    <Grid Background="LightSkyBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Canvas Grid.Row="0">
            <Button Content="ViewCart" Click= "ClickViewCart" Canvas.Right="0"/>
        </Canvas>
        <!--Temporary button to get to next screen-->
        <Canvas Grid.Row="1">
            <Button Content="Neo Geo MVS" Click="ClickCategory" Canvas.Right="50" Canvas.Top="150"/>
        </Canvas>
    </Grid>
</Page>
